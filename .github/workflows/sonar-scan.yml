name: SonarQube Cloud Scan with Dynamic Matrix

on:
  push:
    branches:
      - '**'
      - 'main'
  pull_request:
    branches:
      - 'main'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # ============================================================================
  # Job 1: Detect Changes
  # ============================================================================
  detect-changes:
    name: Detect Changed Modules
    runs-on: ubuntu-latest
    outputs:
      # dorny/paths-filter ĐÃ TỰ ĐỘNG trả về mảng JSON chuẩn (VD: '["tax"]' hoặc '[]')
      # Nên ta lấy luôn giá trị này truyền cho matrix, không cần bash script xử lý thêm
      modules: ${{ steps.filter.outputs.changes }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6 # Đã update

      - name: Filter changed modules
        id: filter
        uses: dorny/paths-filter@v4
        with:
          filters: |
            media: 'media/**'
            product: 'product/**'
            cart: 'cart/**'
            location: 'location/**'
            order: 'order/**'
            customer: 'customer/**'
            rating: 'rating/**'
            inventory: 'inventory/**'
            tax: 'tax/**'
            search: 'search/**'

  # ============================================================================
  # Job 2: Build & Test with Dynamic Matrix
  # ============================================================================
  build-and-test:
    name: Test Module [${{ matrix.module }}]
    needs: detect-changes
    if: ${{ needs.detect-changes.outputs.modules != '[]' && needs.detect-changes.outputs.modules != '' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        module: ${{ fromJson(needs.detect-changes.outputs.modules) }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6 # Đã update

      - name: Set up JDK 25
        uses: actions/setup-java@v5 # Đã update
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Cache Maven packages
        uses: actions/cache@v5 # Đã update
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Run tests and generate Jacoco coverage for ${{ matrix.module }}
        run: |
          mvn clean \
            org.jacoco:jacoco-maven-plugin:prepare-agent \
            test \
            org.jacoco:jacoco-maven-plugin:report \
            -pl ${{ matrix.module }} \
            -am \
            -Dmaven.test.failure.ignore=true \
            --batch-mode \
            --fail-at-end

      - name: Upload Jacoco coverage report for ${{ matrix.module }}
        if: always()
        uses: actions/upload-artifact@v7 # Đã update
        with:
          name: jacoco-report-${{ matrix.module }}
          path: ${{ matrix.module }}/target/site/jacoco/jacoco.xml
          retention-days: 1 
          if-no-files-found: warn

  # ============================================================================
  # Job 3: SonarQube Cloud Scan
  # ============================================================================
  sonarqube-scan:
    name: SonarQube Cloud Scan
    needs: [detect-changes, build-and-test]
    if: always()
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6 # Đã update
        with:
          fetch-depth: 0

      - name: Set up JDK 25
        uses: actions/setup-java@v5 # Đã update
        with:
          java-version: '25'
          distribution: 'temurin'

      - name: Cache Maven packages
        uses: actions/cache@v5 # Đã update
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Create coverage output directory
        run: mkdir -p coverage-reports

      - name: Download Jacoco coverage reports
        # Chỉ cố download nếu Job 2 thực sự đã chạy và tạo file (tránh cảnh báo đỏ lỗi download)
        if: ${{ needs.detect-changes.outputs.modules != '[]' && needs.detect-changes.outputs.modules != '' }}
        uses: actions/download-artifact@v8 # Đã update
        with:
          path: coverage-reports
          pattern: jacoco-report-*
          merge-multiple: false
      - name: Compile project for SonarQube
        run: |
                mvn package \
                  -DskipTests \
                  -Dmaven.main.skip=false \
                  --batch-mode \
                  --fail-at-end

      - name: Prepare SonarQube parameters
        id: sonar-params
        run: |
          COVERAGE_FILES=$(find coverage-reports -name "jacoco.xml" | tr '\n' ',' | sed 's/,$//')
          if [ -z "$COVERAGE_FILES" ]; then
            echo "::notice::No coverage reports found - SonarQube will analyze without coverage"
            echo "coverage-paths=" >> $GITHUB_OUTPUT
          else
            echo "::notice::Using coverage reports: $COVERAGE_FILES"
            echo "coverage-paths=$COVERAGE_FILES" >> $GITHUB_OUTPUT
          fi

      - name: SonarQube Cloud scan
        uses: SonarSource/sonarqube-scan-action@v5.0.0 # Đã là phiên bản mới nhất
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          # -Dsonar.projectKey=tue29092005a_yas
          args: >
            -Dsonar.projectKey=tue29092005a_yas
            -Dsonar.organization=tue29092005a
            ${{ steps.sonar-params.outputs.coverage-paths != '' && format('-Dsonar.coverage.jacoco.xmlReportPaths={0}', steps.sonar-params.outputs.coverage-paths) || '' }}
            -Dsonar.java.binaries=**/target/classes

      - name: List downloaded files (Debug)
        run: ls -R coverage-reports

      - name: SonarQube scan completed
        run: |
          echo "::notice::SonarQube Cloud scan completed successfully"
          echo "View results at: https://sonarcloud.io/project/overview?id=tue29092005a_yas"


          